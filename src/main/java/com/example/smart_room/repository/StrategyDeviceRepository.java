package com.example.smart_room.repository;

import com.example.smart_room.model.StrategyDevice;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StrategyDeviceRepository extends JpaRepository<StrategyDevice, Long> {
}
